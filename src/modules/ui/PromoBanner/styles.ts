import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    width: '100%',
    height: 320,
  },
  image: {
    width: '100%',
    height: '100%',
  },
  text: {
    position: 'absolute',
    bottom: 15,
    left: 15,
  }
});
