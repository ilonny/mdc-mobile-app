import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  touchableWrapper: {
    position: 'absolute',
    width: '100%',
    height: '100%',
  },
  countrySelectLabel: {
    position: 'absolute',
    left: 17,
    top: 5,
  },
  inputWrapper: {
    overflow: 'hidden',
  },
});
