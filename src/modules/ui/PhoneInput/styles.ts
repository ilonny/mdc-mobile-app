import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    width: '100%',
    alignItems: 'center',
    flexDirection: 'row',
    paddingHorizontal: 35,
    paddingLeft: 55,
  },
  textInput: {
    flex: 1,
    // backgroundColor: 'red',
    fontSize: 34,
    paddingLeft: 20,
    color: '#fff',
  },
});
