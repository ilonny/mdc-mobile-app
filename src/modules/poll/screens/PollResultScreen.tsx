import React from 'react';
import { ScreenContainer, Typography } from '../../ui';

export const PollResultScreen = () => {
  return (
    <ScreenContainer headerProps={{ backButton: true }}>
      <Typography.BoldText>PollResultScreen</Typography.BoldText>
    </ScreenContainer>
  );
};
