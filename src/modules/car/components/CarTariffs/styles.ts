import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  perDayWrapper: {
    marginBottom: 5,
  },
  row: {
    paddingVertical: 12,
    borderTopColor: 'transparent',
    borderTopWidth: 1,
  },
  rowBorder: {
    borderTopColor: 'rgba(109, 115, 128, 0.2)',
  },
});
